# TODO

- [x] Update `next.config.js` to set `turbopack.root` to the project directory.
- [x] Run `npm run dev` and confirm Turbopack panic is resolved.
- [x] Add `netlify.toml` to enforce Netlify build command and publish directory (`npm run build:netlify`, `out/`).
- [x] Add redirect fallback in `netlify.toml` for direct route access on static hosting.
- [ ] Redeploy on Netlify and verify routes: `/`, `/admin`, `/admin/hero`, `/admin/content`, `/admin/testimonies`, `/admin/events`, `/admin/bookings`, `/admin/about`, `/admin/footer`.
- [ ] Summarize deployment verification and finalize.
