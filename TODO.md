# TODO

- [x] Update `next.config.js` to set `turbopack.root` to the project directory.
- [x] Run `npm run dev` and confirm Turbopack panic is resolved.
- [ ] Thoroughly test routes: `/`, `/admin`, `/admin/hero`, `/admin/content`, `/admin/testimonies`, `/admin/events`, `/admin/bookings`, `/admin/about`, `/admin/footer`.
- [ ] Summarize test results and finalize.
